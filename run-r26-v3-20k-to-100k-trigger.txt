Retry R26-v3 continuation after diagnosing the 20k-to-40k startup SIGSEGV as an unsafe Fortran optional-logical expression in array3d_sync: present(timerept) .and. timerept. The workflow now replaces all such timerept guards with nested PRESENT/IF blocks, audits that none remain, rebuilds the exact R26-v3 source, and retries 20k to 40k before chaining 60k, 80k, and 100k. Physics, closure equations, wall formulas, grid, timestep, and the validated 20k checkpoint are unchanged. Segmented-restart wall-memory caveat remains.